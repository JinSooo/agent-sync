{
  "name": "agent-sync",
  "version": "0.2.0",
  "description": "Tauri-first cross-platform visual sync studio for Codex and Claude Code agent state.",
  "type": "module",
  "bin": {
    "agent-sync": "./bin/agent-sync.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check ./bin/agent-sync.js && node --check ./src/classify.js && node --check ./src/diff.js && node --check ./src/doctor.js && node --check ./src/plan.js && node --check ./src/surfaces.js",
    "rs:check": "cargo check --workspace",
    "rs:test": "cargo test --workspace",
    "rs:scan": "cargo run -p agent_sync_cli -- scan",
    "desktop:dev": "pnpm --dir apps/desktop tauri:dev",
    "desktop:build": "pnpm --dir apps/desktop tauri:build",
    "desktop:web:build": "pnpm --dir apps/desktop build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
