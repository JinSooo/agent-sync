{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Agent Sync Studio default desktop capability. The frontend only reaches narrow Rust commands registered by the app.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
